{
  "name": "@sahilhasnain/android-ci",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "android-ci": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "dev": "tsx src/cli.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "kleur": "^4.1.5",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
